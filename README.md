# rust-aide-demo

```shell
cargo init --bin --name rust-aide-demo
```

## cargo-make

Install:

```shell
cargo install cargo-make
```

Examples:

```shell
cargo make check
cargo make test
cargo make ci
cargo make db-up
cargo make db-migrate
```
